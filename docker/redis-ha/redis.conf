# Redis configuration for Ocean Shopping Center distributed locks

# Basic settings
daemonize no
port 6379
bind 0.0.0.0
protected-mode no

# Memory settings
maxmemory 1gb
maxmemory-policy allkeys-lru

# Persistence settings
# RDB snapshots
save 900 1
save 300 10
save 60 10000
dbfilename dump.rdb

# AOF (Append Only File)
appendonly yes
appendfilename appendonly.aof
appendfsync everysec
no-appendfsync-on-rewrite no
auto-aof-rewrite-percentage 100
auto-aof-rewrite-min-size 64mb

# Networking
tcp-keepalive 60
timeout 0
tcp-backlog 511

# Security
# requirepass will be set via command line

# Logging
loglevel notice
logfile ""

# Slow log
slowlog-log-slower-than 10000
slowlog-max-len 128

# Client settings
client-output-buffer-limit normal 0 0 0
client-output-buffer-limit replica 256mb 64mb 60
client-output-buffer-limit pubsub 32mb 8mb 60

# Replication settings
replica-serve-stale-data yes
replica-read-only yes
repl-diskless-sync no
repl-diskless-sync-delay 5
repl-ping-replica-period 10
repl-timeout 60
repl-disable-tcp-nodelay no
repl-backlog-size 1mb
repl-backlog-ttl 3600

# Advanced settings
hash-max-ziplist-entries 512
hash-max-ziplist-value 64
list-max-ziplist-size -2
list-compress-depth 0
set-max-intset-entries 512
zset-max-ziplist-entries 128
zset-max-ziplist-value 64
hll-sparse-max-bytes 3000

# Active rehashing
activerehashing yes

# Limits
maxclients 10000

# Lua scripting
lua-time-limit 5000

# Dangerous commands disabled for security
rename-command FLUSHDB ""
rename-command FLUSHALL ""
rename-command DEBUG ""

# Enable keyspace notifications for distributed lock monitoring
notify-keyspace-events "Ex"